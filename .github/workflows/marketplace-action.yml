name: marketplace-action
on:
  push:
    branches: 
      - 'work/actions-practice'
jobs:
  marketplace:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v6.1.0
      - name: Print statement in Python
        run: |
          echo "Hello Python"
          pyhton -version
  
          
          
        
      
